# Deprecated: standalone admin app

The admin console now lives in **`apps/web`** at `/admin` (same origin as the main site).

Use `npm run dev -w @landscrape/web` and open http://localhost:3000/admin.

This package is kept temporarily for reference; it is no longer built or deployed.
