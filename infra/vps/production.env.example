# Production .env is NOT committed. See .env.vps.example for variable names.
#
# GitHub Actions (auto-deploy): repository secret VPS_DOTENV = full file contents.
# Manual deploy from your Mac (optional):
#   LANDSCRAPE_VPS_ENV_FILE=~/.landscrape-vps.env ./scripts/deploy-vps.sh deploy
# One-time VPS bootstrap:
#   cp .env.vps.example .env   # then edit secrets on the server
